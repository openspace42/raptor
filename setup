#!/bin/bash

# Set bash environment error management

set -e
set -u

################################################################################

source_dir="$( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd )"

( cd $source_dir && git checkout tags/v0.0.1 && bash ./install )
